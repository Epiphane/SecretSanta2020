define(["require", "exports", "../lib/juicy", "./helpers/keys", "../lib/three.js", "./states/load_screen"], function (require, exports, juicy_1, keys_1, THREE, load_screen_1) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    const GAME_WIDTH = 1280;
    const GAME_HEIGHT = 720;
    let renderer = new THREE.WebGLRenderer();
    renderer.setPixelRatio(window.devicePixelRatio);
    renderer.setSize(GAME_WIDTH, GAME_HEIGHT);
    document.body.appendChild(renderer.domElement);
    juicy_1.Game.init(renderer, GAME_WIDTH, GAME_HEIGHT, keys_1.default);
    // On window resize, fill it with the game again!
    window.onresize = function () {
        juicy_1.Game.resize();
    };
    juicy_1.Game.setState(new load_screen_1.default()).run();
});
//# sourceMappingURL=main.js.map